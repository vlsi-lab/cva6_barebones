// Copyright lowRISC contributors.
// Licensed under the Apache License, Version 2.0, see LICENSE for details.
// SPDX-License-Identifier: Apache-2.0
//
// Register Top module auto-generated by `reggen`


`include "common_cells/assertions.svh"

module ascon_reg_top #(
    parameter type reg_req_t = logic,
    parameter type reg_rsp_t = logic,
    parameter int AW = 6
) (
  input clk_i,
  input rst_ni,
  input  reg_req_t reg_req_i,
  output reg_rsp_t reg_rsp_o,
  // To HW
  output ascon_reg_pkg::ascon_reg2hw_t reg2hw, // Write
  input  ascon_reg_pkg::ascon_hw2reg_t hw2reg, // Read


  // Config
  input devmode_i // If 1, explicit error return for unmapped register access
);

  import ascon_reg_pkg::* ;

  localparam int DW = 64;
  localparam int DBW = DW/8;                    // Byte Width

  // register signals
  logic           reg_we;
  logic           reg_re;
  logic [AW-1:0]  reg_addr;
  logic [DW-1:0]  reg_wdata;
  logic [DBW-1:0] reg_be;
  logic [DW-1:0]  reg_rdata;
  logic           reg_error;

  logic          addrmiss, wr_err;

  logic [DW-1:0] reg_rdata_next;

  // Below register interface can be changed
  reg_req_t  reg_intf_req;
  reg_rsp_t  reg_intf_rsp;


  assign reg_intf_req = reg_req_i;
  assign reg_rsp_o = reg_intf_rsp;


  assign reg_we = reg_intf_req.valid & reg_intf_req.write;
  assign reg_re = reg_intf_req.valid & ~reg_intf_req.write;
  assign reg_addr = reg_intf_req.addr;
  assign reg_wdata = reg_intf_req.wdata;
  assign reg_be = reg_intf_req.wstrb;
  assign reg_intf_rsp.rdata = reg_rdata;
  assign reg_intf_rsp.error = reg_error;
  assign reg_intf_rsp.ready = 1'b1;

  assign reg_rdata = reg_rdata_next ;
  assign reg_error = (devmode_i & addrmiss) | wr_err;


  // Define SW related signals
  // Format: <reg>_<field>_{wd|we|qs}
  //        or <reg>_{wd|we|qs} if field == 1 or 0
  logic [63:0] data_0_qs;
  logic [63:0] data_0_wd;
  logic data_0_we;
  logic [63:0] data_1_qs;
  logic [63:0] data_1_wd;
  logic data_1_we;
  logic [63:0] data_2_qs;
  logic [63:0] data_2_wd;
  logic data_2_we;
  logic [63:0] data_3_qs;
  logic [63:0] data_3_wd;
  logic data_3_we;
  logic [63:0] data_4_qs;
  logic [63:0] data_4_wd;
  logic data_4_we;
  logic csreg_start_qs;
  logic csreg_start_wd;
  logic csreg_start_we;
  logic csreg_done_qs;
  logic csreg_done_wd;
  logic csreg_done_we;
  logic [3:0] csreg_rounds_qs;
  logic [3:0] csreg_rounds_wd;
  logic csreg_rounds_we;

  // Register instances

  // Subregister 0 of Multireg data
  // R[data_0]: V(False)

  prim_subreg #(
    .DW      (64),
    .SWACCESS("RW"),
    .RESVAL  (64'h0)
  ) u_data_0 (
    .clk_i   (clk_i    ),
    .rst_ni  (rst_ni  ),

    // from register interface
    .we     (data_0_we),
    .wd     (data_0_wd),

    // from internal hardware
    .de     (hw2reg.data[0].de),
    .d      (hw2reg.data[0].d ),

    // to internal hardware
    .qe     (),
    .q      (reg2hw.data[0].q ),

    // to register interface (read)
    .qs     (data_0_qs)
  );

  // Subregister 1 of Multireg data
  // R[data_1]: V(False)

  prim_subreg #(
    .DW      (64),
    .SWACCESS("RW"),
    .RESVAL  (64'h0)
  ) u_data_1 (
    .clk_i   (clk_i    ),
    .rst_ni  (rst_ni  ),

    // from register interface
    .we     (data_1_we),
    .wd     (data_1_wd),

    // from internal hardware
    .de     (hw2reg.data[1].de),
    .d      (hw2reg.data[1].d ),

    // to internal hardware
    .qe     (),
    .q      (reg2hw.data[1].q ),

    // to register interface (read)
    .qs     (data_1_qs)
  );

  // Subregister 2 of Multireg data
  // R[data_2]: V(False)

  prim_subreg #(
    .DW      (64),
    .SWACCESS("RW"),
    .RESVAL  (64'h0)
  ) u_data_2 (
    .clk_i   (clk_i    ),
    .rst_ni  (rst_ni  ),

    // from register interface
    .we     (data_2_we),
    .wd     (data_2_wd),

    // from internal hardware
    .de     (hw2reg.data[2].de),
    .d      (hw2reg.data[2].d ),

    // to internal hardware
    .qe     (),
    .q      (reg2hw.data[2].q ),

    // to register interface (read)
    .qs     (data_2_qs)
  );

  // Subregister 3 of Multireg data
  // R[data_3]: V(False)

  prim_subreg #(
    .DW      (64),
    .SWACCESS("RW"),
    .RESVAL  (64'h0)
  ) u_data_3 (
    .clk_i   (clk_i    ),
    .rst_ni  (rst_ni  ),

    // from register interface
    .we     (data_3_we),
    .wd     (data_3_wd),

    // from internal hardware
    .de     (hw2reg.data[3].de),
    .d      (hw2reg.data[3].d ),

    // to internal hardware
    .qe     (),
    .q      (reg2hw.data[3].q ),

    // to register interface (read)
    .qs     (data_3_qs)
  );

  // Subregister 4 of Multireg data
  // R[data_4]: V(False)

  prim_subreg #(
    .DW      (64),
    .SWACCESS("RW"),
    .RESVAL  (64'h0)
  ) u_data_4 (
    .clk_i   (clk_i    ),
    .rst_ni  (rst_ni  ),

    // from register interface
    .we     (data_4_we),
    .wd     (data_4_wd),

    // from internal hardware
    .de     (hw2reg.data[4].de),
    .d      (hw2reg.data[4].d ),

    // to internal hardware
    .qe     (),
    .q      (reg2hw.data[4].q ),

    // to register interface (read)
    .qs     (data_4_qs)
  );


  // R[csreg]: V(False)

  //   F[start]: 0:0
  prim_subreg #(
    .DW      (1),
    .SWACCESS("RW"),
    .RESVAL  (1'h0)
  ) u_csreg_start (
    .clk_i   (clk_i    ),
    .rst_ni  (rst_ni  ),

    // from register interface
    .we     (csreg_start_we),
    .wd     (csreg_start_wd),

    // from internal hardware
    .de     (hw2reg.csreg.start.de),
    .d      (hw2reg.csreg.start.d ),

    // to internal hardware
    .qe     (),
    .q      (reg2hw.csreg.start.q ),

    // to register interface (read)
    .qs     (csreg_start_qs)
  );


  //   F[done]: 1:1
  prim_subreg #(
    .DW      (1),
    .SWACCESS("RW"),
    .RESVAL  (1'h0)
  ) u_csreg_done (
    .clk_i   (clk_i    ),
    .rst_ni  (rst_ni  ),

    // from register interface
    .we     (csreg_done_we),
    .wd     (csreg_done_wd),

    // from internal hardware
    .de     (hw2reg.csreg.done.de),
    .d      (hw2reg.csreg.done.d ),

    // to internal hardware
    .qe     (),
    .q      (reg2hw.csreg.done.q ),

    // to register interface (read)
    .qs     (csreg_done_qs)
  );


  //   F[rounds]: 5:2
  prim_subreg #(
    .DW      (4),
    .SWACCESS("RW"),
    .RESVAL  (4'h0)
  ) u_csreg_rounds (
    .clk_i   (clk_i    ),
    .rst_ni  (rst_ni  ),

    // from register interface
    .we     (csreg_rounds_we),
    .wd     (csreg_rounds_wd),

    // from internal hardware
    .de     (hw2reg.csreg.rounds.de),
    .d      (hw2reg.csreg.rounds.d ),

    // to internal hardware
    .qe     (),
    .q      (reg2hw.csreg.rounds.q ),

    // to register interface (read)
    .qs     (csreg_rounds_qs)
  );




  logic [5:0] addr_hit;
  always_comb begin
    addr_hit = '0;
    addr_hit[0] = (reg_addr == ASCON_DATA_0_OFFSET);
    addr_hit[1] = (reg_addr == ASCON_DATA_1_OFFSET);
    addr_hit[2] = (reg_addr == ASCON_DATA_2_OFFSET);
    addr_hit[3] = (reg_addr == ASCON_DATA_3_OFFSET);
    addr_hit[4] = (reg_addr == ASCON_DATA_4_OFFSET);
    addr_hit[5] = (reg_addr == ASCON_CSREG_OFFSET);
  end

  assign addrmiss = (reg_re || reg_we) ? ~|addr_hit : 1'b0 ;

  // Check sub-word write is permitted
  always_comb begin
    wr_err = (reg_we &
              ((addr_hit[0] & (|(ASCON_PERMIT[0] & ~reg_be))) |
               (addr_hit[1] & (|(ASCON_PERMIT[1] & ~reg_be))) |
               (addr_hit[2] & (|(ASCON_PERMIT[2] & ~reg_be))) |
               (addr_hit[3] & (|(ASCON_PERMIT[3] & ~reg_be))) |
               (addr_hit[4] & (|(ASCON_PERMIT[4] & ~reg_be))) |
               (addr_hit[5] & (|(ASCON_PERMIT[5] & ~reg_be)))));
  end

  assign data_0_we = addr_hit[0] & reg_we & !reg_error;
  assign data_0_wd = reg_wdata[63:0];

  assign data_1_we = addr_hit[1] & reg_we & !reg_error;
  assign data_1_wd = reg_wdata[63:0];

  assign data_2_we = addr_hit[2] & reg_we & !reg_error;
  assign data_2_wd = reg_wdata[63:0];

  assign data_3_we = addr_hit[3] & reg_we & !reg_error;
  assign data_3_wd = reg_wdata[63:0];

  assign data_4_we = addr_hit[4] & reg_we & !reg_error;
  assign data_4_wd = reg_wdata[63:0];

  assign csreg_start_we = addr_hit[5] & reg_we & !reg_error;
  assign csreg_start_wd = reg_wdata[0];

  assign csreg_done_we = addr_hit[5] & reg_we & !reg_error;
  assign csreg_done_wd = reg_wdata[1];

  assign csreg_rounds_we = addr_hit[5] & reg_we & !reg_error;
  assign csreg_rounds_wd = reg_wdata[5:2];

  // Read data return
  always_comb begin
    reg_rdata_next = '0;
    unique case (1'b1)
      addr_hit[0]: begin
        reg_rdata_next[63:0] = data_0_qs;
      end

      addr_hit[1]: begin
        reg_rdata_next[63:0] = data_1_qs;
      end

      addr_hit[2]: begin
        reg_rdata_next[63:0] = data_2_qs;
      end

      addr_hit[3]: begin
        reg_rdata_next[63:0] = data_3_qs;
      end

      addr_hit[4]: begin
        reg_rdata_next[63:0] = data_4_qs;
      end

      addr_hit[5]: begin
        reg_rdata_next[0] = csreg_start_qs;
        reg_rdata_next[1] = csreg_done_qs;
        reg_rdata_next[5:2] = csreg_rounds_qs;
      end

      default: begin
        reg_rdata_next = '1;
      end
    endcase
  end

  // Unused signal tieoff

  // wdata / byte enable are not always fully used
  // add a blanket unused statement to handle lint waivers
  logic unused_wdata;
  logic unused_be;
  assign unused_wdata = ^reg_wdata;
  assign unused_be = ^reg_be;

  // Assertions for Register Interface
  `ASSERT(en2addrHit, (reg_we || reg_re) |-> $onehot0(addr_hit))

endmodule
